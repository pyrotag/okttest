deploy:
  - kubectl create secret docker-registry pyrotag --docker-server=hub.docker.com
  - kubectl patch serviceaccount default -p '{"imagePullSecrets": [ { "name": "pyrotag" } ] }'
  - okteto build -t pyrotag/okttest:latest
  - kubectl apply -f k8s